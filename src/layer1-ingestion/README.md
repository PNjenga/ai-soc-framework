# Layer 1: Data Ingestion and Normalization

This module handles log ingestion, schema validation, deduplication, timestamping, tokenization, encryption, and normalization into a unified security event format.
